services:
  - type: web
    name: karma-webapp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11